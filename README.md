# Idris2 Respo (minimal port)

This is a tiny, minimal reimplementation of the core idea from `rs-respo` in Idris2.

Build and run (requires `idris2`):

```bash
cd idr-respo
idris2 --build respo.ipkg
./build/exec/respo
```

Run and type `+` then Enter to increment the counter, or `q` to quit.
