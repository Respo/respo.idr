on:
  push:
    branches:
      - main
  pull_request: {}

name: Build and Deploy

jobs:
  build_and_deploy:
    name: Build and Deploy
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Install Idris2
        run: |
          wget https://github.com/idris-lang/Idris2/releases/download/v0.8.0/idris2-0.8.0-x86_64-linux.tar.gz
          tar -xzf idris2-0.8.0-x86_64-linux.tar.gz
          sudo cp -r idris2-0.8.0/* /usr/local/
          idris2 --version

      - name: Build project
        run: |
          idris2 --codegen javascript --build respo.ipkg

      - name: Prepare distribution
        run: |
          mkdir -p dist
          cp index.html dist/
          cp build/exec/respo dist/respo.js
          # Create a minimal package for deployment
          ls -la dist/

      - name: Deploy to server
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        id: deploy
        uses: Pendect/action-rsyncer@v2.0.0
        env:
          DEPLOY_KEY: ${{secrets.rsync_private_key}}
        with:
          flags: "-avzr --progress"
          options: ""
          ssh_options: ""
          src: "dist/*"
          dest: "rsync-user@tiye.me:/web-assets/repo/${{ github.repository }}"

      - name: Display status from deploy
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: echo "${{ steps.deploy.outputs.status }}"
